# Shared non-secret runtime defaults for local LeLamp services.
# Keep the temporary development token in one place so the Web Console and
# collaboration server always use the same fallback value.
LELAMP_DEFAULT_WEB_TOKEN=lelamp-local-console
